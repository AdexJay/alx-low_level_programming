#include <stdlib.h>
#include <time.h>
/* more headers goes in here */
#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always return an integer
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z' ; a++) putchar(a);
	putchar('\n');
	return (0);
}
